// Thin re-export: FeedFilters is an alias for FeedHeader (which already manages
// the 3 feed tabs: School / For You / Following).
export { default as FeedFilters } from './FeedHeader';
export { default } from './FeedHeader';
